let arr=[2,4,6,8]
console.log(arr.push(10));//5
// console.log(arr.toString());

//push() method actually adds the element in the last position but it returns the length of the array after adding the new element.