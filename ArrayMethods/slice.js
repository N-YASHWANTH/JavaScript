let arr=[0,1,2,3,4,5]
console.log(arr.slice(0,2));//[0,1]
console.log(arr.slice(2));//[2,3,4,5]

//When we use slice() method with 2 parameters it prints the elements that are sliced.

//when we use slice() method with 1 parameter it prints the elements except the sliced elements.